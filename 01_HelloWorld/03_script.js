const width = +prompt("enter the width") // + is for conversion of string to number 
const height = +prompt ("enter the height ") 
const areaRec = width * height; 
console.log(areaRec);

console.log(width * height);
document.write(`Rectangle Area: ${width * height}`); // to show the js output on the webpage it is a shortcut 